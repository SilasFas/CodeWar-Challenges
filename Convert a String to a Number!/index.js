/*
We need a function that can transform a string into a number (integer).
*/
const stringToNumber = function (str) {
  let number = Number(str)
  return number;
}


console.log(stringToNumber('15'))


